# Progress — Mailroom Repo Extraction

## Reboot check (refreshed 2026-04-25 post-execution)

1. **Where am I?** Waves 0–5 complete with deviations documented; Wave 5.5 (branch cleanup) added and complete. Production runtime healthy on the new image; ConfigFiles main is in sync with origin/ConfigFiles main; `gitlab.com/jmcstone/mailroom` exists with full 79-commit history.
2. **Where am I going?** Wave 6 — lode migration. Move 7 mailroom-belonging plans + the 4 infrastructure docs from nanoclaw's lode into `~/Projects/mailroom/lode/`. Update both lodes' lode-map.md to cross-reference correctly. This is the highest-risk lode operation; should approach carefully.
3. **What is the goal?** Mailroom source + lode + plans all live in their own GitLab repo. ConfigFiles' `containers/mailroom/` is a thin compose stack only. Three-repo split (nanoclaw + mailroom + ConfigFiles) is operational with clear interface contracts.
4. **What have I learned?** Origin/ConfigFiles main had been quietly emptied of all mailroom files (source + compose + env.vault) by a destructive merge prior to this session; mechanism unknown but likely a prior agent session that resolved a merge conflict badly. Detected only when we ran `git pull --ff-only` and saw 28k lines deleted. Pivoted Option A (accept origin's deletion as canonical, layer fresh ADD commit on top) to recover cleanly without force-push. The 4 brief-audit commits stop being ConfigFiles-side history but are preserved as content in the new mailroom repo. `git subtree split` is a viable substitute for `git-filter-repo` when filter-repo isn't installed and you don't need full multi-branch extraction. GitLab auto-protects `main` on new projects, blocking force-push until UI toggle.
5. **What have I done?** Subtree-split mailroom out of ConfigFiles (79 commits, paths re-rooted), pushed to new `gitlab.com/jmcstone/mailroom` repo with force-push (after temporary protection disable). Built `mailroom-local:latest` from the new repo location, ran the full test suite (421 tests + tsc) in a one-shot container — green. Recreated production containers on the new image, verified clean startup and active email processing during cutover. Wrote new ConfigFiles compose-stack README. Slimmed ConfigFiles' compose file (dropped `build:` from 4 services). Reset ConfigFiles main to origin (which already had source deleted from the anomaly) and created fresh ADD commit (`6c26600`) with just the 3 runtime files. Pushed fast-forward to origin. Deleted three local-only ConfigFiles branches (fix/morning-brief-audit, madison-read-power, mailroom-extracted) — nanoclaw branches kept per Jeff's "leave around just in case" preference.

## Sessions

### 2026-04-25 — plan authored

- Surfaced from a discussion about getting `fix/morning-brief-audit` merged to main and the bigger structural problem that mailroom (a sizable application) lives inside ConfigFiles (a config-snippets repo).
- Investigated `~/containers/`, `~/Projects/ConfigFiles/`, and the bridge container. Confirmed: ConfigFiles is 587 MB / 230 MB-in-containers; bridge has no on-disk message cache; nanoclaw is the working precedent for "real repo not symlink, source lives outside ConfigFiles."
- Ran a 90-day git-history pairing analysis. 5 mailroom-active days, 5 of 5 also nanoclaw-active, 0 mailroom-only days. Drilled into the 98-commit nanoclaw burst: ~6% integration code, ~94% lode bookkeeping for mailroom plans whose trackers live in nanoclaw's lode. Confirms the diagnosis that the split (with lode migration) reduces real coupling rather than introduces it.
- Walked through the four implementation decisions: GitLab (matches ConfigFiles), `latest` tag (personal-instance pattern), verify-and-close old plan (done in same session), plan name `mailroom-repo-extraction` (distinguishes from the closed `mailroom-extraction` plan).
- Closed predecessor plan: ticked M6.3 (ports stanza removed — verified in compose), M7.1–M7.5 (lode graduation mapped to existing infrastructure docs), deferred M8/M9 to a new tech-debt entry `TD-MAIL-FRESH-INSTALL-SKILLS`. Moved plan to `complete/`.
- Drafted this tracker's 7 waves: branch greenup → repo extraction → image build/push → ConfigFiles compose slim-down → cutover → lode migration → verification.

### 2026-04-25 — Waves 0–5 + 5.5 executed

- Started Wave 0 with `git pull --ff-only origin main` on ConfigFiles. Discovered origin/main had silently lost all of `containers/mailroom/mailroom/` via a strange merge chain (28k lines / 116 files deleted). Origin push log showed Jeff's email but no other machine was active. Most likely cause: a prior Claude/agent session resolved a merge conflict by accepting "delete" without understanding the implications.
- Pivoted to Option A: accept origin's deletion-state as canonical, layer fresh cleanup commit on top.
- Reset local ConfigFiles main back to `e221f14` (pre-pull state), then ff-merged `fix/morning-brief-audit` (`602d297`) to set the working tree up with full mailroom source.
- Used `git subtree split --prefix=containers/mailroom/mailroom -b mailroom-extracted` (filter-repo not installed; subtree split is built into git core). Produced 79-commit `mailroom-extracted` branch with paths re-rooted.
- Set up `~/Projects/mailroom/` via `git init` + `git fetch <local ConfigFiles> mailroom-extracted` + `git checkout -b main FETCH_HEAD` + `git remote add origin gitlab.com:jmcstone/mailroom.git`.
- First push rejected: GitLab auto-protects `main` on new projects against force-push. Jeff disabled protection, force-push succeeded (`dfbd621..85163f2`), Jeff re-enabled protection.
- Built `mailroom-local:latest` from `~/Projects/mailroom/` via `docker compose build`. All layers cache-hit (source byte-identical). Image SHA `2bc976daf4ba`.
- Ran `npm ci + tsc --noEmit + vitest` in a one-shot `node:22-slim` container with source bind-mounted read-only. Result: 421 tests passed, 2 skipped, 0 failures, tsc green. No host pollution.
- Edited ConfigFiles' `containers/mailroom/mailroom/docker-compose.yml`: dropped `build: .` from all 4 services (ingestor, inbox-mcp, backfill-proton, backfill-gmail), kept `image: mailroom-local`, added file-header comment explaining the source split.
- Wrote `containers/mailroom/mailroom/README.md` with build-deploy workflow + pointer to new repo.
- Cutover: `env-vault env.vault -- docker compose up -d` recreated both running containers cleanly. New image (`2bc976daf4ba`) replaced old (`c871a921b19a`/`fcfab8a34bdc`). Both containers came up healthy within 28 seconds. Ingestor immediately resumed processing real Proton IDLE events (Built In, American Express, BookBub messages classified and emitted as ipc-out events during the cutover window). No mail dropped.
- Reset local ConfigFiles main to `origin/main` (`6fc1fc7`), restored only the 3 runtime files we want (compose, env.vault, README), committed as `6c26600`, fast-forward pushed to origin (`6fc1fc7..6c26600`). No force-push, no protection toggle on ConfigFiles.
- Wave 5.5: deleted local-only ConfigFiles branches `fix/morning-brief-audit`, `madison-read-power`, `mailroom-extracted`. ConfigFiles now has only `main`. Nanoclaw branches kept per Jeff's preference.
- Updated this tracker (Waves 0–5 marked done with deviation notes; Errors table populated; Current status reflects post-execution state). Wave 6 (lode migration) and Wave 7 (24h soak + closure) still pending.
