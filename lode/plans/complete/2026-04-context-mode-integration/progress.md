# Progress — Context-Mode Integration

## 2026-04-20

- **22:00** — Plan created. User ("Jeff") asked what context-mode actually does after the trawl tracker's original "defer" decision. Realized the feature is broader than session memory — it's a full context-compression layer (98% reduction claim via PreToolUse interception + sandboxed subprocesses).
- **22:05** — Confirmed Agent SDK supports `hooks:` option (already used for PreCompact at `container/agent-runner/src/index.ts:662`). Integration is feasible.
- **22:10** — Created plan directory with tracker.md + findings.md. Phase 0 is "discuss & decide" — six open questions around env handling, data isolation, hook ordering, sandbox runtimes, rollout, and Trawl-interaction.
- **22:15** — Next action: run `/lode:discuss` to work through open questions.
- **22:40** — `/lode:discuss` complete. Five gray areas presented, user locked all five: npm+env install, mount-sentinel opt-in, two-hook PreCompact, Node+Python+shell sandbox, all-four-groups rollout. Phase 0 done. Next: `/lode:plan`.
- **23:05** — `/lode:plan` complete. Planner decomposed the remaining 7 phases into 8 acceptance criteria + ~15 wave-grouped tasks. Verification flagged three gaps: (a) Phase 4 Wave 2 target under-specified → tightened to name `scripts/group-config.ts` DB-edit mechanism; (b) Phase 6 was a no-op → replaced with per-group smoke test (send test message from each of the 4 groups); (c) Phase 7 missing tech-debt + trawl tracker cleanup → added. Next: `/lode:execute`.
- **23:30** — Reviewed the `CLAUDE_PLUGIN_ROOT` plumbing decision with user. Replaced original "set env var + hardcode path in index.ts" with "resolve path at runtime via `createRequire` + `require.resolve('context-mode/package.json')`, wrapped in a `createContextModeHook(scriptName)` factory that returns `HookCallback` functions." Rationale: Agent SDK takes HookCallback functions anyway (not shell-command strings), so we're wrapping `spawn` either way — asking Node's module resolver for the path is more robust than hardcoding. Updated tracker Decisions table, AC-1 and AC-3, Phase 1 task, Phase 2 task (added new factory task); updated findings.md Locked Decisions + Area 1 Gray Area Explored.
- **00:10** — `/lode:execute` Wave A. Spawned 3 parallel executors: Dockerfile (→ commit `96e241d` ✓), agent-runner wiring, and filesystem dirs. User interrupted the last two to verify the host path — I had proposed `~/containers/data/NanoClaw/extra/context-mode/{avp,main,trading,inbox}/` as BTRFS subvolumes, which was double-wrong: (a) a-mem's actual pattern is top-level `~/containers/data/NanoClaw/a-mem/telegram_*/` (no `extra/`), and (b) those per-group dirs are regular directories, not subvolumes. Corrected the plan (AC-5, AC-6, Phase 4 Wave 1+2 task text, findings.md). Removed stray `/home/jeff/containers/data/NanoClaw/extra/` left by the interrupted executor. Created the 4 per-group dirs directly (trivial, no executor needed): `~/containers/data/NanoClaw/context-mode/{telegram_avp,telegram_main,telegram_trading,telegram_inbox}/` with `jeff:jeff` and mode 2775. AC-5 satisfied. Phase 1 Wave 1 + Phase 4 Wave 1 now complete. Re-spawning the agent-runner executor next.
- **00:25** — Kept the interrupted executor's partial agent-runner edits (they were clean — imports, factory, sentinel, mcpServers entry, allowedTools), and completed them directly: (a) fixed MCP server `command` from `context-mode` CLI → `node <root>/start.mjs` which is the actual MCP server entry per the plugin's `package.json` bin/main split; (b) extracted `resolveCtxModeRoot()` helper so path logic is shared between MCP reg and hook factory; (c) added full `hooks:` block wiring — PreToolUse (8 matchers via regex alternation), PostToolUse (upstream broad matcher), SessionStart, UserPromptSubmit, all conditional on `hasContextMode`, plus PreCompact array chains `createPreCompactHook` + `createContextModeHook('precompact')`. Typecheck `npx tsc --noEmit` clean. Commit `abcbacc`. Phase 2 Wave 1 done.
- **00:45** — Wave B complete. Rebuilt image, applied context-mode mounts to all 4 groups via new `scripts/context-mode-defaults.ts` helper (`fbc78fd`), extended `~/.config/nanoclaw/mount-allowlist.json` with 4 entries, restarted service. Validation uncovered two container gotchas requiring a second rebuild: (a) globally-installed npm packages aren't on Node's default resolution path from `/app/dist/` — fixed with `ENV NODE_PATH=/usr/local/lib/node_modules`; (b) Node 22 strict `exports` enforcement means `require.resolve('context-mode/package.json')` fails because context-mode doesn't export that subpath — fixed by walking `require.resolve.paths()` and reading package.json from disk directly (`3e768dd`). After second rebuild, three-layer validation: direct MCP server spawn test, resolve-from-within-container test, and full agent-runner smoke test. AC-1 through AC-6 green; AC-7/AC-8 pending real-message test.
- **01:30** — Real-message test on Madison's main group revealed three more gotchas: (a) Agent SDK's pipe-separated matcher string doesn't fire reliably — fixed by switching to per-matcher entries; (b) `hookSpecificOutput.additionalContext` is dropped by the Agent SDK (not forwarded to LLM context like Claude Code does) — fixed by vendoring context-mode's SKILL.md into `container/skills/context-mode/` so routing guidance lives in the skill system prompt; (c) MCP server command must use `node <pkg>/start.mjs` not the `context-mode` CLI binary (was fixed earlier but worth documenting). Vendored skill uses bare tool names (`ctx_execute`) so no patching needed for our MCP registration name. All changes committed in `5a41482`.
- **02:00** — Live smoke test on Main with a "find TODO comments" prompt. First attempt: Madison still used raw Grep because her session predated the skill install (sessions resume their original skill list). Cleared session row in `sqlite3 .../store/messages.db` → `sessions` table. Second attempt: Madison activated context-mode skill, used `mcp__context-mode__ctx_execute` twice — first a `grep -rin "TODO"` sandbox call, then a `ls + find` sandbox call. PreToolUse/PostToolUse hooks fire, file_search events recorded to FTS5, a-mem + Trawl still work. Integration end-to-end confirmed for Main group. AC-1 through AC-8 all green.

## 5-question reboot check

1. **Where am I?** Phase 0 — plan seeded, design decisions not yet locked. No code touched.
2. **Where am I going?** Next milestone: resolve the six open questions in findings.md / tracker.md, lock decisions in the decisions table, then run `/lode:plan` to decompose into executable waves.
3. **What is the goal?** Integrate context-mode into the NanoClaw agent container so Madison's sessions get ~98% context-window savings on tool outputs plus BM25-backed state restoration across compactions.
4. **What have I learned?** Context-mode is NOT just session memory — it's a PreToolUse-driven context-compression layer. The hooks are the feature; the MCP tools are passive without them. Claude Code plugin hooks use `CLAUDE_PLUGIN_ROOT` env var which the Agent SDK doesn't set automatically — this is the most delicate integration point.
5. **What have I done?** Plan skeleton created at `lode/plans/active/2026-04-context-mode-integration/` with tracker + findings seeded from investigation of the plugin's hooks.json, SKILL.md, and README.

## Test results

| Test | Status | Notes |
|------|--------|-------|
| _(none yet — Phase 0)_ | | |

## Errors

_(none yet)_
